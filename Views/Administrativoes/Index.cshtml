@model IEnumerable<SistemaCE.Models.Administrativo>

@{
    ViewData["Title"] = "Administrativos";
}
<link rel="stylesheet" href="~/css/index_global.css" />

<div class="page-container">

    <div class="header">
        <h1>Administrativos</h1>

        <a asp-action="Create" class="btn-create">
            + Nuevo registro
        </a>
    </div>

    <div class="table-container">
        <table class="custom-table">
            <thead>
                <tr>
                    <th>Departamento</th>
                    <th>Puesto</th>
                    <th>RFC</th>
                    <th>Sueldo</th>
                    <th>Usuario</th>
                    <th>Empleado</th>
                    <th>Acciones</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Departamento</td>
                        <td>@item.Puesto</td>
                        <td>@item.Rfc</td>

                        <td>
                            @string.Format(System.Globalization.CultureInfo.GetCultureInfo("es-MX"), "{0:C}", item.Sueldo)
                        </td>

                        <td>
                            @string.Join(" ",
                                item.IdAdministrativoNavigation?.Nombre ?? "",
                                item.IdAdministrativoNavigation?.ApellidoPaterno ?? "",
                                item.IdAdministrativoNavigation?.ApellidoMaterno ?? ""
                            )
                        </td>

                        <td>
                            @item.NumeroEmpleadoNavigation?.TipoEmpleado
                        </td>

                        <td class="actions">
                            <a asp-action="Edit" asp-route-id="@item.IdAdministrativo">Editar</a>
                            <a asp-action="Details" asp-route-id="@item.IdAdministrativo">Ver</a>
                            <a asp-action="Delete" asp-route-id="@item.IdAdministrativo">Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>

        </table>
    </div>

</div>